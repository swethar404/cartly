import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ================= PRIMARY BRAND =================

  static const Color primary = Color(0xFFF97316);
  static const Color primaryDark = Color(0xFFEA580C);
  static const Color primaryLight = Color(0xFFFFEDD5);

  // ================= BACKGROUND =================

  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;

  // ================= TEXT =================

  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Color(0xFFFFFFFF);

  // ================= STATUS =================

  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // ================= BORDERS =================

  static const Color border = Color(0xFFE5E7EB);

  // ================= DASHBOARD =================

  static const Color revenue = Color(0xFF16A34A);
  static const Color orders = Color(0xFF2563EB);
  static const Color inventory = Color(0xFFF97316);
  static const Color analytics = Color(0xFF9333EA);

  // ================= HEALTH SCORE =================

  static const Color excellent = Color(0xFF22C55E);
  static const Color good = Color(0xFF84CC16);
  static const Color average = Color(0xFFFACC15);
  static const Color poor = Color(0xFFEF4444);
}